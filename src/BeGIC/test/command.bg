window 500,500

p = {"x" : 250, "y" : 50}
have red
for x = 50 to 450 step 10 do
	line(p["x"], p["y"]),(x, 450)
end

puts "\"" + "\""

have blue
pi = 3.14159265
a = -1 + 2
puts "\""
drowSin = {
	for x = 0.00000000 to 500 step 0.1 do
		point(x,sin(x / 500 * 2 * pi) * 100 + y)
	end
}
for y = 200 to 250 step 5 do
	call drowSin
end

have blue + red
call (drowSin + {y = y + 5}) * 10

have green
t = 0.00000000
while t < 2 * pi {
	point(cos(3*t) * 100 + 250, sin(2*t) * 100 + 250)
	t = t + pi / 180 * 0.1
}

puts "ok"
